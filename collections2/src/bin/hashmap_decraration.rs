use std::collections::HashMap;

fn main() {
    let mut months: HashMap<String, i32> = HashMap::new();
    let mut months12: HashMap<String, i32> = HashMap::with_capacity(12);
    //months.insert("January", 1);
    //months.insert("February", 2);
    //months.insert("March", 3);
    //months.insert("April", 4);
    //println!("{:?}", months);
}
